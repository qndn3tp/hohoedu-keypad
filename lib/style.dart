import 'package:flutter/material.dart';

const PRIMARY_BLUE = Color(0xff3c46ff);
const TEXT_GREY = Color(0xff393E46);

var theme = ThemeData(
  // 배경색
  scaffoldBackgroundColor: Colors.white,

  textTheme: const TextTheme(
    
  ),

);